
const init_state = [
  {'id': '', 'statusIcon':'', 'name': '', 'telephoneNumber': '', 'timeStamp': '' }
]
export default function counter(state = init_state, action) {
  switch (action.type) {
    default:
      return state;
  }
}
